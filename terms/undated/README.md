# Undated

Work by a previous student whose term is not recorded.

## `term-project/`

A stock market technical-analysis project on NSE India (Nifty 50) data.

| File | What it is |
|------|-----------|
| `20190808004.ipynb` | The main notebook — data acquisition, preprocessing, technical indicators, and machine learning models (LightGBM, CatBoost, scikit-learn) |
| `economics-term.ipynb` | A second, larger notebook |
| `main.py` | Candlestick plotting with mplfinance and Plotly |
| `data/HDFCBANK.csv` | One stock's OHLCV history |
| `requirements.txt` | Empty — the notebook installs its dependencies inline with `!pip install` |

The filename carries the student number `20190808004`; the author's name is known
but is not written here. Add it if you want it recorded.

Libraries used: `tradingview_ta`, `stockstats`, `yahoofinancials`, `yfinance`,
`pandas_ta`, `finta`, `TA-Lib`, `plotly`, `mplfinance`, `scikit-learn`, `lightgbm`,
`catboost`.

Read it as a worked example of what a term project can look like — not as
something to hand in.

Data source: [NSE India / Nifty 50 on Kaggle](https://www.kaggle.com/datasets/rohanrao/nifty50-stock-market-data)
